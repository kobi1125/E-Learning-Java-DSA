<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.elearningapp"> <!-- Replace with your actual package name -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ELearningApp"
        tools:targetApi="31">

        <!-- MainActivity is the entry point of the app -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity for user registration -->
        <activity android:name=".RegisterActivity" android:exported="true"></activity>

        <!-- LoginActivity for user login -->
        <activity android:name=".LoginActivity" android:exported="true"></activity>

        <!-- HomeActivity as the target for a successful login -->
        <activity android:name=".HomeActivity" android:exported="true"></activity>


        <!-- ✅ Add this line for Learning Modules screen -->
        <activity android:name=".LearningModulesActivity" android:exported="true" />

        <activity android:name=".ModuleDetailActivity" android:exported="true" />

        <activity android:name=".QuizActivity" android:exported="true" />

        <activity android:name=".CertificateSuccessActivity" />




    </application>

</manifest>
